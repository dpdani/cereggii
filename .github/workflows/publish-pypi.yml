name: publish to pypi

on:
  workflow_dispatch:
  release:
    types:
      - published

jobs:
  publish:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version:
          - 3.13
    permissions:
      id-token: write  # OIDC publishing https://docs.pypi.org/trusted-publishers/adding-a-publisher/

    steps:
      - name: download wheels to distribute
        uses: dawidd6/action-download-artifact@v2
        with:
          commit: ${{ github.sha }}
          name: cibw-wheels-.*
          name_is_regexp: true
          path: dist
          if_no_artifact_found: fail
      - name: download source to distribute
        uses: dawidd6/action-download-artifact@v2
        with:
          commit: ${{ github.sha }}
          name: cibw-sdist
          name_is_regexp: true
          path: dist
          if_no_artifact_found: fail

      - name: publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          repository-url: https://test.pypi.org/legacy/
