name: tests

on:
  push:
    branches: [ "main", "dev" ]
  pull_request:

jobs:
  build:

    runs-on: ubuntu-20.04
    strategy:
      fail-fast: false
      matrix:
        python-version: [ "nogil-3.9" ]

    steps:
      - uses: actions/checkout@v3
      - name: set up python ${{ matrix.python-version }}
        uses: dpdani/setup-python@nogil
        with:
          python-version: ${{ matrix.python-version }}
      - name: system info
        run: |
          uname -a
          cat /proc/cpuinfo
      - name: check nogil
        run: |
          python -c "import sys; print(sys.flags.nogil);"
      - name: install dependencies & cereggii
        run: |
          python -m pip install --upgrade pip
          pip -V
          pip install .[dev]
      - name: test with pytest
        run: |
          ulimit -c unlimited
          ulimit -c
          mkdir -p $PWD/cores
          echo "$PWD/cores" | sudo tee /proc/sys/kernel/core_pattern
          sudo cat /proc/sys/kernel/core_pattern
          (sudo -H $pythonLocation/bin/python -m pytest --junitxml=junit/test-results-${{ matrix.python-version }}.xml) || (ls -al ./cores && exit 1)
        if: ${{ always() }}
      - name: ls
        if: ${{ always() }}
        run: |
          ls -al
          ls -al ./cores
      - name: upload core dump file
        uses: actions/upload-artifact@v3
        with:
          name: core-dump
          path: /home/runner/work/cereggii/cereggii/cores
        if: ${{ always() }}
      - name: upload pytest test results
        uses: actions/upload-artifact@v3
        with:
          name: pytest-results-${{ matrix.python-version }}
          path: junit/test-results-${{ matrix.python-version }}.xml
        if: ${{ always() }}
